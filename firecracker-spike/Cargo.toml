[package]
name = "firecracker-spike"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "io-util"] }
serde_json = "1"

[profile.release]
lto = "thin"
strip = true
